# Frontend - Agente Cultural

Frontend React + TypeScript para o Agente Cultural de IA.

## 🚀 Início Rápido

1. Instale as dependências:
```bash
npm install
```

2. Configure a URL da API (opcional, padrão é `http://localhost:8000/api/v1`):
```env
VITE_API_BASE_URL=http://localhost:8000/api/v1
```

3. Execute o servidor de desenvolvimento:
```bash
npm run dev
```

Acesse: http://localhost:3000

## 📁 Estrutura

- `src/api/` - Cliente da API
- `src/views/` - Telas (ChatView, AdminView)
- `src/state/` - Estado global (Zustand)

## 🎨 Telas

- `/chat` - Interface conversacional com o agente
- `/admin` - Painel de administração

## 🛠️ Tecnologias

- React 18
- TypeScript
- Vite
- React Router
- TanStack Query (React Query)
- Zustand
- React Markdown
- Tailwind CSS

